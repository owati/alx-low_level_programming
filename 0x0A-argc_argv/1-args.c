#include <stdio.h>

/**
 * main - prints the number of argurments
 * @argc: the lenght of the commandlinr
 * @argv: the command line arguements
 * Return: 0
*/

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
